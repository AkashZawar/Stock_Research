"""App config for the Market Monitor tab."""
from django.apps import AppConfig


class MarketMonitorConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "market_monitor"
